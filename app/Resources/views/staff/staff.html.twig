{% extends 'base.html.twig' %}

{% block nav %}

    <div class="nav-item nav-link app-user-username">{{ app.user.username }}</div>
    <a class="nav-item nav-link" href="{{ path('staff_newsletter') }}" id="newsletter">Newsletter</a>
    <a class="nav-item nav-link" href="{{ path('staff_offers_list') }}" id="offers">Offers</a>
    <a class="nav-item nav-link" href="{{ path('staff_gamesession') }}" id="new-gamesession">New game session</a>
    <a class="nav-item nav-link" href="{{ path('logout') }}" id="logout">logout</a>

{% endblock %}